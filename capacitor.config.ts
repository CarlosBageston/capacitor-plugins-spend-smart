import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plugin.spendsmart',
  appName: 'capacitor-plugins-spend-smart',
  webDir: 'dist'
};

export default config;
